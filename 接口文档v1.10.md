# 情绪日记 API 接口文档 v1.10

> 上一阶段，本文档从原始 HTML 对话记录中提取，描述前端 ⇄ 后端的对外 API 接口约定，当时共计12个接口。
> 现阶段，本文档依照实际UI设计图纸，对接口进行新增补充，目前接口数量已来到近30个，并部署了云服务器。

---

版本历史：

| 版本    | 日期         | 修改人 | 说明                                                                                          |
| ----- | ---------- | --- | ------------------------------------------------------------------------------------------- |
| v1.0  | 2025-08-01 | 全体  | 初版，确定全部接口约定                                                                                 |
| v1.1  | 2026-08-05 | 李海宇 | 更改后端用户接口基础路径去掉api                                                                           |
| v1.2  | 2026-08-26 | 李海宇 | 删除 moodEmoji 字段（前后端均不再使用）                                                                   |
| v1.3  | 2026-08-26 | 李海宇 | 响应示例补齐 title 字段（接口3/4/5），请求参数表补 title 说明                                                    |
| v1.4  | 2026-08-26 | 李海宇 | id 示例由 ObjectId 风格字符串改为数值型自增 id，对齐 MySQL 实现                                                 |
| v1.5  | 2026-08-29 |     | 补充本轮新增接口(14~20)；标注词云/AI鼓励暂不需要；附接口状态总览与路径一致性提示                                               |
| v1.6  | 2026-08-29 |     | 补充接口21：情绪频率；修改了接口8和9有关userId获取的问题                                                           |
| v1.7  | 2026-08-30 |     | 新增模块六「标签功能」接口22~25；接口3、18 补充 tags 字段；同步更新接口状态总览                                             |
| v1.8  | 2026-08-30 |     | 废弃 keywords 字段：接口3/4/5 响应移除 keywords，统一改用 tags；算法文档标注 data.keywords 后端暂不使用；接口11 词云标注数据来源已失效 |
| v1.9  | 2026-08-30 |     | 删除算法接口文档，移除 title 字段：对照 UI 设计稿确认日记不需要标题，接口3/4/5/18/19 的请求体、参数表与响应示例全部删除 title               |
| v1.10 | 2026-08-30 |     | 新增接口26~29：关键词搜索/今日心情侧栏"去年今天"/我的日记：卡片上滑/个人中心导出数据(Excel)          |

---

## 〇、全局约定（所有接口通用）

- 基础路径： http://localhost:8080/
- 云服务器路径：http://121.43.104.168:8080/
- 数据格式： application/json; charset=utf-8
- 统一响应格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { }
}
```

- 业务状态码：

| code | 含义          | HTTP状态码 |
| ---- | ----------- | ------- |
| 200  | 成功          | 200     |
| 400  | 参数错误        | 400     |
| 401  | 未登录/Token过期 | 401     |
| 403  | 无权限         | 403     |
| 404  | 资源不存在       | 404     |
| 500  | 服务器内部错误     | 500     |

- 认证方式： 除接口1、2外，所有接口请求头携带 Authorization: Bearer <token>

---

## 模块一：用户认证

---

### 接口 1：用户注册

| 项目        | 内容        |
| --------- | --------- |
| 方法        | POST      |
| 路径        | /register |
| 是否需要Token | 否         |

请求参数（Body）：

```json
{
  "username": "zhangsan",
  "password": "123456",
  "confirmPassword": "123456",
  "nickname": "张三"
}
```

| 字段              | 类型     | 必填  | 限制                      |
| --------------- | ------ | --- | ----------------------- |
| username        | string | 是   | 3~16位，只能包含字母、数字、下划线     |
| password        | string | 是   | 6~20位                   |
| confirmPassword | string | 是   | 必须与password一致           |
| nickname        | string | 否   | 1~20个字符，缺省时默认等于username |

成功响应：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "zhangsan",
    "nickname": "张三",
    "avatar": "",
    "createdAt": "2025-08-01T09:00:00Z"
  }
}
```

失败响应：

```json
// 用户名已被注册
{ "code": 400, "message": "用户名已存在", "data": null }
// 两次密码不一致
{ "code": 400, "message": "两次输入的密码不一致", "data": null }
// 参数校验失败
{ "code": 400, "message": "用户名长度需在3~16位之间", "data": null }
```

后端注意事项：  密码加密存储（BCrypt）， confirmPassword  只用于后端校验，不存储。

---

### 接口 2：用户登录

| 项目        | 内容     |
| --------- | ------ |
| 方法        | POST   |
| 路径        | /login |
| 是否需要Token | 否      |

请求参数（Body）：

```json
{
  "username": "zhangsan",
  "password": "123456"
}
```

成功响应：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx",
    "expiresIn": 86400,
    "userInfo": {
      "id": 1,
      "username": "zhangsan",
      "nickname": "张三",
      "avatar": "",
      "createdAt": "2025-08-01T09:00:00Z"
    }
  }
}
```

| 字段        | 说明                           |
| --------- | ---------------------------- |
| token     | JWT，有效期24小时（86400秒）          |
| expiresIn | Token有效期（秒），前端可用于本地存储时记录过期时间 |

失败响应：

```json
// 用户名或密码错误
{ "code": 400, "message": "用户名或密码错误", "data": null }
```

前端注意事项：  登录成功后把  token  和  userInfo  存入 localStorage，后续所有请求自动带上  Authorization  头。前端根据  expiresIn  提前判断Token是否过期，提前跳转登录页。

---

## 模块二：情绪日记（CRUD）

---

> ⚠️ **v1.8 字段变更（重要）**：DiaryVO（接口 3/4/5/18/19 的返回体）**已移除 `keywords` 字段**，统一改用 `tags`。
> 
> - 原因：`keywords` 本应由算法服务从正文自动抽取，但算法侧一直未返回该字段；词云（接口 11）也随之改为「用户手动打标签」方案（见模块六）。
> - 前端凡读取 `keywords` 的地方一律改读 `tags`；该篇日记没打标签时 `tags` 为 `[]`，**不会是 null**。
> - `tags` 由用户在写日记时手动提交，与算法无关，算法降级时照常返回。

> ⚠️ **v1.9 字段变更（重要）**：对照 UI 设计稿确认日记没有标题，**DiaryVO 与接口 3/18 的请求体均已移除 `title` 字段**。
> 
> - 影响范围：接口 3（添加，请求体+响应）、接口 4（列表）、接口 5（详情）、接口 18（修改，请求体）、接口 19（按日期查询）。
> - 前端提交日记时**不要再带 `title`**；后端已忽略该字段，传了也不会落库、不会回显。
> - 这是**回退 v1.3 的补齐**（v1.3 曾为接口 3/4/5 补上 title），UI 定稿后确认不需要。

---

### 接口 3：添加日记

| 项目        | 内容       |
| --------- | -------- |
| 方法        | POST     |
| 路径        | /diaries |
| 是否需要Token | 是        |

请求参数（Body）：

```json
{
  "content": "今天和朋友去了公园，心情很好，就是有点累。",
  "moodType": "happy",
  "tags": ["朋友", "放松"]
}
```

| 字段       | 类型           | 必填  | 限制                                                         |
| -------- | ------------ | --- | ---------------------------------------------------------- |
| content  | string       | 是   | 1~500字                                                     |
| moodType | string       | 是   | 枚举： happy  /  sad  /  angry  /  anxious  /  tired  /  calm |
| tags     | string[]（数组） | 否   | 用户为该篇日记手动打的标签，见「模块六：标签功能」                                  |

> tags 说明：
> 
> - 数组元素为字符串，单个标签最多 4 个字（由后端标签库限制，见接口 23）
> - 后端会做清洗：去空、去前后空格、去重，一篇日记最多保留 10 个
> - 不传或传 `[]` 表示该篇日记不打标签，返回时 `tags` 为 `[]`
> - **标签内容不需要先在「我的标签库」中存在**，可以直接打任意标签（宽松模式）

成功响应：

```json
{
  "code": 200,
  "message": "日记保存成功",
  "data": {
    "id": 7,
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
      "label": "positive",
      "score": 0.87,
      "emotion": "joy"
    },
    "tags": ["朋友", "放松"],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

失败响应：

```json
// 内容为空或超过500字
{ "code": 400, "message": "日记内容需在1~500字之间", "data": null }
// moodType不合法
{ "code": 400, "message": "情绪类型不合法", "data": null }
// 未登录
{ "code": 401, "message": "请先登录", "data": null }
```

异常降级场景（重要）：

```json
{
  "code": 500,
  "message": "日记已保存，但情感分析暂不可用，请稍后在列表页刷新查看",
  "data": {
    "id": 7,
    "content": "……",
    "moodType": "happy",
    "sentiment": null,
    "tags": ["朋友", "放松"],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

> 约定：  当算法服务调用失败时，日记仍然保存成功， sentiment  置为  null ，但返回码为  500  并提示“日记已保存”。前端收到500时，检查  data.id  是否存在，若存在则提示用户“日记已保存，情绪分析稍后刷新”， 避免用户以为保存失败重复提交 。
> 
> **tags 不受降级影响**：标签是用户手动提交的，与算法无关，算法挂了也照常入库并原样回显（上例中仍返回 `["朋友", "放松"]`），不要置空。

前端注意事项：  此接口算法耗时可能超过1秒，点击保存后显示 loading 状态，并防止重复 点击（防抖）。

后端注意事项：  先保存日记（开启事务），再调用算法服务。若算法失败，记录日志并做降级返回。确保用户只能操作自己的日记（从 Token 解析 userId）。

---

### 接口 4：获取日记列表（分页）

| 项目        | 内容                  |
| --------- | ------------------- |
| 方法        | GET                 |
| 路径        | /diaries            |
| 是否需要Token | 是                   |
| 说明        | 获取当前登录用户的日记，按创建时间倒序 |

请求参数（Query）：

```text
GET /api/diaries?page=1&pageSize=10&startDate=2025-07-01&endDate=2025-07-31&moodType=happy
```

| 字段        | 类型     | 必填  | 说明                |
| --------- | ------ | --- | ----------------- |
| page      | int    | 否   | 页码，默认1，从1开始       |
| pageSize  | int    | 否   | 每页条数，默认10，最大50    |
| startDate | string | 否   | 开始日期  YYYY-MM-DD  |
| endDate   | string | 否   | 结束日期  YYYY-MM-DD  |
| moodType  | string | 否   | 按用户选择的情绪筛选，不传则为全部 |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "total": 56,
    "page": 1,
    "pageSize": 10,
    "totalPages": 6,
    "list": [
      {
        "id": 7,
      "content": "今天和朋友去了公园，心情很好，就是有点累。",
      "moodType": "happy",
      "sentiment": {
          "label": "positive",
          "score": 0.87,
          "emotion": "joy"
        },
        "tags": ["朋友", "放松"],
        "createdAt": "2025-08-01T10:30:00Z"
      },
      {
        "id": 6,
      "content": "考试没考好，好难过。",
      "moodType": "sad",
      "sentiment": {
          "label": "negative",
          "score": 0.21,
          "emotion": "sadness"
        },
        "tags": ["考试"],
        "createdAt": "2025-07-31T22:00:00Z"
      }
    ]
  }
}
```

> 约定：  列表中的  content  由后端直接截取前50个字（因为列表页展示摘要），完整内容通过接口5获取。附带省略号，前端无需再截取。

失败响应：

```json
// 页码不合法
{ "code": 400, "message": "页码必须大于0", "data": null }
```

前端注意事项：  分页组件页码从  page  开始，滚动加载或点击分页时更换  page  参数。查询条件变化时重置  page=1 。

后端注意事项：  查询必须带  userId  过滤，索引建议： {userId: 1, createdAt: -1} 。

---

### 接口 5：查看单条日记详情

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | GET               |
| 路径        | /api/diaries/{id} |
| 是否需要Token | 是                 |

请求参数（路径参数）：

```text
GET /api/diaries/7
```

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 7,
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
      "label": "positive",
      "score": 0.87,
      "emotion": "joy"
    },
    "tags": ["朋友", "放松"],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

失败响应：

```json
// 日记不存在
{ "code": 404, "message": "日记不存在", "data": null }
// 试图访问别人的日记
{ "code": 403, "message": "无权查看该日记", "data": null }
```

后端注意事项：  查询时同时验证  id  存在 且  userId  匹配，否则分别返回404或403。

---

### 接口 6：日历视图（按天情感汇总）

| 项目        | 内容                    |
| --------- | --------------------- |
| 方法        | GET                   |
| 路径        | /api/diaries/calendar |
| 是否需要Token | 是                     |

请求参数（Query）：

```text
GET /api/diaries/calendar?year=2025&month=8
```

| 参数    | 类型  | 必填  | 说明       |
| ----- | --- | --- | -------- |
| year  | int | 是   | 年份，如2025 |
| month | int | 是   | 月份，1~12  |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-08-01", "avgScore": 0.87, "count": 1, "mainMood": "happy" },
    { "date": "2025-08-03", "avgScore": 0.35, "count": 3, "mainMood": "sad" },
    { "date": "2025-08-05", "avgScore": 0.52, "count": 2, "mainMood": "calm" }
  ]
}
```

| 字段       | 说明                            |
| -------- | ----------------------------- |
| date     | 日期，格式  YYYY-MM-DD             |
| avgScore | 当天所有日记情感得分的平均值（0~1）           |
| count    | 当天日记条数                        |
| mainMood | 当天出现次数最多的 moodType（用户主动选择的情绪） |

> 约定：  没有写日记的日期 不返回 （前端默认显示空白）。 avgScore >= 0.6  为积极， 0.4 ~ 0.6  为中性， < 0.4  为消极，用于前端给日历格子着色。

前端注意事项：  用返回的  date  字段直接映射到日历组件。不需要补全整个月的每一天。

后端注意事项：  若一天有多个日记，取  moodType  出现次数最多的那个；若并列，取最近一条的。

---

### 接口 7：删除日记

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | DELETE            |
| 路径        | /api/diaries/{id} |
| 是否需要Token | 是                 |

请求参数（路径参数）：

```text
DELETE /api/diaries/7
```

成功响应：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

失败响应：

```json
// 日记不存在
{ "code": 404, "message": "日记不存在", "data": null }
// 权限不足
{ "code": 403, "message": "无权删除该日记", "data": null }
```

前端注意事项：  删除操作需要二次确认弹窗（防误删）。删除成功后，本地列表移除该条并刷新统计页数据。

---

## 模块三：统计与可视化

---

### 接口 8：统计总览

| 项目        | 内容                 |
| --------- | ------------------ |
| 方法        | GET                |
| 路径        | /api/stats/summary |
| 是否需要Token | 是                  |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalDiaryCount": 56,
    "activeDays": 30,
    "sentimentRatio": {
      "positive": 0.55,
      "neutral": 0.25,
      "negative": 0.20
    },
    "averageScore": 0.62,
    "streakDays": 5,
    "longestStreak": 12,
    "currentMoodSummary": {
      "mostFrequentMood": "happy",
      "mostFrequentMoodCount": 18
    }
  }
}
```

| 字段                 | 说明                     |
| ------------------ | ---------------------- |
| totalDiaryCount    | 总日记数                   |
| activeDays         | 写日记的总天数                |
| sentimentRatio     | 积极/中性/消极占比，总和为1        |
| averageScore       | 所有日记平均情感得分             |
| streakDays         | 当前连续写日记天数（今天或昨天有记录才计算） |
| longestStreak      | 历史最长连续天数               |
| currentMoodSummary | 出现频率最高的 moodType 及次数   |

前端注意事项：  这个数据用于“我的统计”页顶部卡片展示。 sentimentRatio  可直接传入 ECharts 饼图。

后端注意事项： streakDays  计算逻辑：从今天开始往前数，如果今天是最后一天且当天有日记则算，否则从昨天开始。连续中断则停止。这个逻辑容易出错，建议单独写工具类并测试。

---

### 接口 9：情绪趋势（折线图）

| 项目        | 内容               |
| --------- | ---------------- |
| 方法        | GET              |
| 路径        | /api/stats/trend |
| 是否需要Token | 是                |

请求参数（Query）：

```text
GET /api/stats/trend?range=7
```

| 参数    | 类型  | 必填  | 说明              |
| ----- | --- | --- | --------------- |
| range | int | 是   | 7 或 30，其他值返回400 |

成功响应（range=7）：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-07-26", "avgScore": 0.45, "diaryCount": 2 },
    { "date": "2025-07-27", "avgScore": 0.62, "diaryCount": 1 },
    { "date": "2025-07-28", "avgScore": 0.30, "diaryCount": 3 },
    { "date": "2025-07-29", "avgScore": 0.81, "diaryCount": 2 },
    { "date": "2025-07-30", "avgScore": 0.55, "diaryCount": 1 },
    { "date": "2025-07-31", "avgScore": 0.48, "diaryCount": 2 },
    { "date": "2025-08-01", "avgScore": 0.77, "diaryCount": 2 }
  ]
}
```

> 约定：  不写日记的日期也返回， avgScore  为  null ， diaryCount  为  0 。这样前端折线图能显示断点，不需要前端自己补全日期。

前端注意事项：  折线图X轴直接用  date ，Y轴用  avgScore 。 null  值用 ECharts 的  connectNulls: false  显示断线。

---

### 接口 10：情绪分布（饼图）

| 项目        | 内容                              |
| --------- | ------------------------------- |
| 方法        | GET                             |
| 路径        | /api/stats/emotion-distribution |
| 是否需要Token | 是                               |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "name": "开心", "value": 18, "moodType": "happy", "color": "#FFD700" },
    { "name": "平静", "value": 10, "moodType": "calm", "color": "#87CEEB" },
    { "name": "焦虑", "value": 12, "moodType": "anxious", "color": "#FFA500" },
    { "name": "难过", "value": 8, "moodType": "sad", "color": "#6495ED" },
    { "name": "愤怒", "value": 5, "moodType": "angry", "color": "#FF4500" },
    { "name": "疲惫", "value": 3, "moodType": "tired", "color": "#808080" }
  ]
}
```

| 字段       | 说明                      |
| -------- | ----------------------- |
| name     | 中文名称，用于前端显示图例           |
| value    | 该情绪出现的次数（按 moodType 统计） |
| moodType | 英文枚举值，用于前端做颜色映射兜底       |
| color    | 建议颜色，前端也可以用自己的映射不用此字段   |

前端注意事项：  支持 ECharts 饼图的  data.name  /  data.value  直接传入。可增加显示百分比。

后端注意事项：  按  moodType  分组统计所有日记，只统计用户自己的记录。

---

### 接口 11：词云数据

| 项目        | 内容                    |
| --------- | --------------------- |
| 方法        | GET                   |
| 路径        | /api/stats/word-cloud |
| 是否需要Token | 是                     |

请求参数：  无（返回全部历史日记的关键词汇总）

> ⚠️ **v1.8 数据来源已失效（重要）**：本接口的数据来自 `diary_keyword` 表，该表的内容由**算法服务返回的 `keywords` 写入**。
> 现在算法侧不返回 `keywords`、后端也已停止写入该表，所以**当前本接口实际返回空数组 `[]`**。
> 关键词的替代方案是**用户手动打标签**（模块六 接口 22~25；统计用接口 25 高频标签）。
> 若将来要做词云，需先推动算法组补 `keywords`，本接口才能恢复数据。

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "word": "焦虑", "value": 24 },
    { "word": "失眠", "value": 18 },
    { "word": "考试", "value": 15 },
    { "word": "开心", "value": 12 },
    { "word": "朋友", "value": 9 },
    { "word": "运动", "value": 7 }
  ]
}
```

| 字段    | 说明              |
| ----- | --------------- |
| word  | 关键词文本           |
| value | 词频（所有日记中出现次数总和） |

> 约定：  只返回出现次数 ≥ 3 的词，且最多返回 50 个，按  value  从高到低排序。过滤掉无意义的停用词（“的”、“了”、“我”等，算法组在提取时就过滤掉）。
> 
> （上述约定在 `diary_keyword` 有数据时才成立，当前该表无数据 → 返回 `[]`）

前端注意事项：  直接传入  echarts-wordcloud  的  data  属性即可。词云大小建议使用  value  映射  textSize 。若数组为空，前端显示“写几篇日记后再来看看你的心情词云吧”空状态提示。

后端注意事项：  这个查询较重的做法是每次实时聚合所有日记的关键词，小数据量没问题。若数据量大可定期缓存（如每天一次）。为了演示速度够快即可。
另注：  本接口**不读 `diary.tags`**——标签统计走接口 25（高频标签），两者数据源不同，别混用。

---

### 接口 12：日历热力图

| 项目        | 内容                 |
| --------- | ------------------ |
| 方法        | GET                |
| 路径        | /api/stats/heatmap |
| 是否需要Token | 是                  |

请求参数（Query）：

```text
GET /api/stats/heatmap?month=2025-08
```

| 参数    | 类型     | 必填  | 说明          |
| ----- | ------ | --- | ----------- |
| month | string | 是   | 格式  YYYY-MM |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-08-01", "score": 0.87, "count": 1 },
    { "date": "2025-08-02", "score": null, "count": 0 },
    { "date": "2025-08-03", "score": 0.35, "count": 3 }
  ]
}
```

| 字段    | 说明                        |
| ----- | ------------------------- |
| date  | YYYY-MM-DD                |
| score | 当天平均情感得分（0~1），没有日记则为 null |
| count | 当天日记条数                    |

> 说明：  热力图是给日历图上色用的，返回该月每一天的数据。 score  为空表示该天无日记，前端可显示灰色或不显示。颜色映射：0 0.4 红，0.4 0.6 黄，0.6~1 绿。此接口与接口6相似，区别是：接口6返回的是 用户主动选择的情绪 （moodType）用于显示表情图标，本接口返回的是 情感分析得分 用于给色块上色。

前端注意事项：  热力图组件可用 GitHub 贡献图样式或普通日历组件，根据  score  值映射颜色深浅。

---

## 模块四：AI 扩展功能

---

### 接口 13：生成鼓励语（可选实现）

| 项目        | 内容                                     |
| --------- | -------------------------------------- |
| 方法        | POST                                   |
| 路径        | /api/ai/encourage                      |
| 是否需要Token | 是                                      |
| 说明        | 根据用户最近7天的日记情感趋势，生成一句个性化的鼓励语（可作为首页顶部横幅） |

请求参数（Body）：  无需传参（后端根据用户历史数据自行分析）

```json
{}
```

成功响应：

```json
{
  "code": 200,
  "message": "生成成功",
  "data": {
    "message": "最近你记录了很多关于考试的压力，试着每天给自己10分钟放空的时间，你已经做得很棒了。",
    "tone": "caring",
    "basedOn": {
      "positiveCount": 2,
      "negativeCount": 5,
      "dominantEmotion": "anxious"
    }
  }
}
```

| 字段      | 说明                                        |
| ------- | ----------------------------------------- |
| message | 生成的鼓励文本，前端直接展示                            |
| tone    | 语气类型（caring / cheerful / calm），前端可配相应文案样式 |
| basedOn | 后端生成逻辑的依据，便于前端调试                          |

实现方式（不调用大模型的简单规则版）：  后端统计最近7天：

- 如果积极（positive）占多数 → 返回 “你的情绪正在变好，继续保持呀！”
- 如果消极（negative）占多数 → 提取高频标签，拼成类似“最近你常提到“考试”，记得给自己一些休息时间，你已经很努力了。”（v1.8 修订：原写「提取高频关键词」，因算法不返回 `keywords`，改为取 `diary.tags` 里出现次数最多的标签）
- 如果中性为主 → “生活就像白开水，平淡也是一种美好。”

失败响应：

```json
// 数据不足（最近7天没有日记）
{ "code": 400, "message": "写几篇日记才能生成专属鼓励哦", "data": null }
```

---

## 附一：协议中几个容易忽略的约定

1. 日期格式： 接口输入统一 YYYY-MM-DD ，后端返回统一 YYYY-MM-DDTHH:mm:ssZ （ISO 8601 UTC时间）。
2. 字段命名： 接口 JSON 字段一律使用 camelCase （如 moodType ），数据库字段可自由设计在内部转换。
3. 空值处理： 字段可能为空的，统一返回 null ，不要返回空字符串（接口6、12的 score 为空时返回 null ）。
4. 列表排序： 所有列表接口默认按 createdAt 倒序。
5. 内容截断： 问题：列表中的 content 返回前50字时，若恰好切在emoji中间会乱码，建议后端按 字符 截取而不是按字节截取，并在末尾加“…”（共53个字符）。
6. id 类型： 所有 id（用户 id / 日记 id）为数值型（MySQL BIGINT 自增），JSON 中以 **数字** 返回，无引号。前端不要按字符串处理。

## 附二：接口依赖关系图（开发顺序）

```text
接口1、2（注册登录）
    ↓
接口3（添加日记） ← 依赖算法服务
    ↓
接口4、5、7（列表/详情/删除） → 接口6、8、9、10、11、12（统计，依赖日记数据）
    ↓
接口13（AI鼓励，依赖统计结果）
```

开发建议：

- 第一阶段（第1周）：实现接口1、2、3、4、5、7 → 完成“能写日记、能看日记、能删日记”
- 第二阶段（第2周）：实现接口6、8、9、10、11、12 → 完成图表展示
- 第三阶段（第3周）：实现接口13，优化算法效果 → 加分项
- 第四阶段（第4周）：联调、完善异常处理、写测试

---

这份文档写完后，建议团队做两件事：

1. 把这份 Markdown 内容导入 Apifox ，添加 Mock 规则，前端第二天就能开始开发。
2. 每个接口标注负责人：前端谁对接、后端谁实现、算法谁支撑、预估完成日期，方便进度跟踪。

如果这份模板对你们有帮助，我可以继续补充“算法服务对后端”的内部接口文档（虽然前端看不到，但后端和算法组需要）。需要的话告诉我。

---

## 模块五：本轮新增接口（v1.5开始）

### 接口 14：修改密码

| 项目        | 内容             |
| --------- | -------------- |
| 方法        | POST           |
| 路径        | /user/password |
| 是否需要Token | 是              |

请求参数（Body）：

```json
{
  "oldPassword": "123456",
  "newPassword": "abcdef",
  "confirmPassword": "abcdef"
}
```

| 字段              | 类型     | 必填  | 限制                |
| --------------- | ------ | --- | ----------------- |
| oldPassword     | string | 是   | 原密码               |
| newPassword     | string | 是   | 6~20 位            |
| confirmPassword | string | 否   | 需与 newPassword 一致 |

成功响应：`{ "code": 200, "message": "密码修改成功，请重新登陆", "data": null }`

后端注意事项：  修改成功后前端须引导用户重新登录（密码已变）。

---

### 接口 15：修改资料（昵称）

| 项目        | 内容            |
| --------- | ------------- |
| 方法        | PUT           |
| 路径        | /user/profile |
| 是否需要Token | 是             |

请求参数（Body）：`{ "nickname": "新昵称" }`

| 字段       | 类型     | 必填  | 限制         |
| -------- | ------ | --- | ---------- |
| nickname | string | 是   | 1~20 字符，非空 |

成功响应：`{ "code": 200, "message": "昵称修改成功", "data": null }`

---

### 接口 16：上传头像

| 项目        | 内容                          |
| --------- | --------------------------- |
| 方法        | POST                        |
| 路径        | /user/avatar                |
| 是否需要Token | 是                           |
| 数据格式      | multipart/form-data（非 JSON） |

请求参数（form-data）：`file`（File，必填，jpg/jpeg/png/webp，≤ 2MB）

成功响应：

```json
{ "code": 200, "message": "操作成功", "data": "/uploads/avatars/xxxxxxxx.jpg" }
```

| 字段   | 说明                                               |
| ---- | ------------------------------------------------ |
| data | 头像相对 URL，前端拼服务器地址（如 `http://localhost:8080`）即可访问 |

前端注意事项：  用 FormData 上传，不要放进 JSON Body；返回的是相对路径，需自行拼接域名。

---

### 接口 17：获取当前用户信息

| 项目        | 内容         |
| --------- | ---------- |
| 方法        | GET        |
| 路径        | /user/info |
| 是否需要Token | 是          |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "username": "zhangsan",
    "nickname": "张三",
    "avatar": "/uploads/avatars/xxx.jpg",
    "createdAt": "2025-08-01T09:00:00"
  }
}
```

前端注意事项：  个人中心页进入时调用，替代“登录时缓存的 userInfo”，避免昵称/头像改后不同步。

---

### 接口 18：修改日记

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | PUT               |
| 路径        | /api/diaries/{id} |
| 是否需要Token | 是                 |

请求参数（Body）：

```json
{
  "content": "修改后的正文，可重新触发情感分析",
  "moodType": "happy",
  "tags": ["朋友", "放松"]
}
```

| 字段       | 类型           | 必填  | 说明                                                       |
| -------- | ------------ | --- | -------------------------------------------------------- |
| content  | string       | 是   | 1~500 字；传新正文会重新调用算法重算情感                                  |
| moodType | string       | 否   | 不传则保留原心情                                                 |
| tags     | string[]（数组） | 否   | **全量覆盖**：传了就整体替换；不传（或传 null）则保持原有标签不变；传 `[]` 表示清空该篇日记的标签 |

成功响应：  同接口 5（DiaryVO，含最新 sentiment 与 tags，**不含 keywords / title**）。

后端注意事项：  content 非空才更新正文并重算情感；只改心情不会覆盖情感结果。

> **删除某个标签的做法**：前端传「去掉该标签后的完整新数组」即可，不需要单独的删除接口。
> 例：原标签 `["学习","压力"]`，想删掉「压力」→ 传 `{"content":"...","tags":["学习"]}`。

---

### 接口 19：按日期查询日记

| 项目        | 内容                           |
| --------- | ---------------------------- |
| 方法        | GET                          |
| 路径        | /api/diaries?date=YYYY-MM-DD |
| 是否需要Token | 是                            |

请求参数（Query）：`GET /api/diaries?date=2026-08-26`

| 字段   | 类型     | 必填  | 说明                       |
| ---- | ------ | --- | ------------------------ |
| date | string | 否   | YYYY-MM-DD，缺省 2026-01-01 |

成功响应：  同接口 5（单篇 DiaryVO，**不含 keywords / title**）；该日无日记时 data 为 null。

前端注意事项：  情绪日历点击某天时调用，拿该日日记详情（配合接口 6 日历视图）。

---

### 接口 20：心情类型列表

| 项目        | 内容              |
| --------- | --------------- |
| 方法        | GET             |
| 路径        | /api/mood/types |
| 是否需要Token | 是               |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    { "code": "happy", "name": "开心", "color": "#FFD700", "emoji": "😊" },
    { "code": "calm",  "name": "平静", "color": "#87CEEB", "emoji": "😌" }
  ]
}
```

前端注意事项：  写日记页“心情选择器”的数据源。color 由后端返回，前端直接用，**无需前端维护颜色对照表**。

---

### 接口 21：情绪频率

| 项目        | 内容                              |
| --------- | ------------------------------- |
| 方法        | GET                             |
| 路径        | /api/stats/emotion-distribution |
| 是否需要Token | 是                               |

请求参数（Query）：

```text
GET /api/stats/emotion-distribution?days=7/30/all
```

| 字段   | 类型     | 必填  | 说明                                            |
| ---- | ------ | --- | --------------------------------------------- |
| days | string | 否   | 天数范围，只能是 `7` / `30` / `all`，默认 `7`；传其他值返回 400 |

> ⚠️ **与接口 10（情绪分布）共用同一路径**，靠是否携带 `days` 参数区分：
> 
> - **不带参数** → 命中接口 10，返回分布数组 `[{name, value, moodType, color}]`
> - **带 `days`** → 命中接口 21（本接口），返回频次 Map
> 
> 前端对接时务必确认参数名是 `days`，不要写成 `range`（接口 9 趋势用的才是 `range`）。
> 
> 三个选项遵照当前的前端 UI 设计图设置，如有需求可改。

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "happy": 5,
    "sad": 1
  }
}
```

| 字段   | 类型     | 说明                                            |
| ---- | ------ | --------------------------------------------- |
| data | object | 键为 moodType（如 happy / sad），值为该情绪出现的**次数（数字）** |

失败响应：

```json
// days 取值非法
{ "code": 400, "message": "只能查询三种情况: 7/30/all", "data": null }
```

> 此处仅举例。data 格式遵循 `{"moodType": count}`，**count 是数字，不是字符串**。
> 
> - 实际返回哪些 moodType 由数据库决定——若选定范围内不存在某种情绪，则不会返回该键值对
> - 若范围内一篇日记都没有，data 为空对象 `{}`

---

### 接口 26：关键词搜索

| 项目        | 内容           |
| --------- | ------------ |
| 方法        | GET          |
| 路径        | /api/diaries |
| 是否需要Token | 是            |

请求参数（Query）：

```text
GET /api/diaries?keywords="关键词"&page=1&pageSize=10
```

| 字段       | 类型     | 必填  | 说明        |
| -------- | ------ | --- | --------- |
| keyword  | string | 是   | 若空参则返回400 |
| page     | int    | 否   | 默认1，从1开始  |
| pageSize | int    | 否   | 默认10      |

> **搜索**：采用MySQL中的全文索引(FullText)，按照日记创建时间排序，依次展示。返回数据参考日记列表获取，将符合关键词搜索的日记以日记列表形式呈现。

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
    {
      "id": 7,
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
        "label": "positive",
        "score": 0.87,
        "emotion": "joy"
      },
      "tags": ["朋友", "放松"],
      "createdAt": "2025-08-01T10:30:00Z"
    },
    ...
    ],
    "total": "10",
    "page": "1",
    "pageSize": "10"
  }
}
```

| 字段   | 类型          | 说明            |
| ---- | ----------- | ------------- |
| data | DiaryPageVO | 返回类型跟获取日记列表相同 |

失败响应：

```json
// keyword 为空
{ "code": 400, "message": "关键词不能为空", "data": null }
```

> 若搜索结果中一篇符号要求的日记都没有，data中list 为空对象 `{}`

---

### 接口27：今日心情侧栏"去年今天"

> 可在前端调用接口19(按日期查询)，将参数设置为一年前的今天

---

### 接口28：我的日记：卡片上滑

| 项目        | 内容           |
| --------- | ------------ |
| 方法        | GET          |
| 路径        | /api/diaries |
| 是否需要Token | 是            |

请求参数（Query）：

```text
GET /api/diaries/{id}/prev-next
```

提示：

> 本文将前一篇日记和后一篇日记放在同一个集合里，依照索引顺序区分。一般情况下，后一篇日记id减去前一篇日记id应等于2。

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
    {
      "id": 1,
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
        "label": "positive",
        "score": 0.87,
        "emotion": "joy"
      },
      "tags": ["朋友", "放松"],
      "createdAt": "2025-08-01T10:30:00Z"
    },
    {
      "id": 3,
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
        "label": "positive",
        "score": 0.87,
        "emotion": "joy"
      },
      "tags": ["朋友", "放松"],
      "createdAt": "2025-08-01T10:30:00Z"
    }
    ],
    "total": "10",
    "page": "1",
    "pageSize": "10"
  }
}
```

| 字段   | 类型            | 说明                |
| ---- | ------------- | ----------------- |
| data | List<DiaryVO> | 前一篇、后一篇共计两篇日记存储于此 |

失败响应：

```json
{ "code": 500, "message": "服务器内部错误", "data": null }
```

**data数据说明：**

> 若id为1，data中list在索引0处为空对象，索引1处有对象；
> 若id为最大id，data中list在索引1处为空对象，索引0处有对象；
> 若1 < id < max，data中list在索引0和1处均有对象。此时，索引0处为前一篇日记，索引1处为后一篇日记。

---

### 接口29：个人中心导出数据(Excel)

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | POST              |
| 路径        | /api/export/excel |
| 是否需要Token | 是                 |

请求参数（Query）：

```text
POST /api/export/excel
```

> 导出的excel有四个字段，分别是日记ID(diaryId)、日记内容(content)、创建时间(createdAt)、情绪种类(moodType)。

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": "已导出excel, 请查看浏览器弹窗"
}
```

| 字段   | 类型     | 说明    |
| ---- | ------ | ----- |
| data | String | 仅响应提示 |

失败响应：

```json
{ "code": 500, "message": "服务器内部错误", "data": null }
```

> 正常反馈应为浏览器出现下载.xlsx文件的弹窗

---

## 模块六：标签功能（v1.7 新增）

> 设计来源：写日记页「标记今天的状态」+ 首页「本周小回顾 → 高频标签」。
> 
> 数据库新增user_tag表

### ⚠️ 两套数据要分清（前端对接时最容易混淆）

| 数据    | 存储位置          | 含义                 | 相关接口        |
| ----- | ------------- | ------------------ | ----------- |
| 我的标签库 | `user_tag` 表  | 「我**可以选**哪些标签」     | 接口 22/23/24 |
| 日记的标签 | `diary.tags`列 | 「这篇日记**实际打了**哪些标签」 | 接口 3/18/25  |

**在标签库里新增/删除标签，不会影响任何一篇历史日记**；反之，写日记时打的标签也不会自动进入标签库。两者互相独立，各管各的。

---

### 接口 22：我的标签库

| 项目        | 内容        |
| --------- | --------- |
| 方法        | GET       |
| 路径        | /api/tags |
| 是否需要Token | 是         |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": ["工作", "学习", "情感", "家庭", "健康", "压力", "治愈", "内耗", "阅读", "睡眠"]
}
```

| 字段   | 说明                      |
| ---- | ----------------------- |
| data | 标签名数组（string[]），按添加顺序返回 |

> **懒初始化约定（重要）**：  用户第一次调用本接口时，若其标签库为空，后端会自动灌入 10 个系统预置标签（工作/学习/情感/家庭/健康/压力/治愈/内耗/阅读/睡眠）。之后完全由用户自己增删，后端不会再补。
> 
> 因此**前端不需要自己维护一份默认标签列表**，直接展示本接口返回的内容即可。

失败响应：

```json
// 未登录或 Token 过期
{ "code": 401, "message": "未登录", "data": null }
```

前端注意事项：  写日记页进入时调用，渲染成一排可勾选的标签 chips。

后端注意事项：  userId 从 Token 解析，不由前端传参（防越权）。

---

### 接口 23：新增标签

| 项目        | 内容        |
| --------- | --------- |
| 方法        | POST      |
| 路径        | /api/tags |
| 是否需要Token | 是         |

请求参数（Body）：

```json
{ "tagName": "运动" }
```

| 字段      | 类型     | 必填  | 限制                  |
| ------- | ------ | --- | ------------------- |
| tagName | string | 是   | 非空，最多 **4 个字**，不可重复 |

成功响应：

```json
{ "code": 200, "message": "操作成功", "data": null }
```

失败响应：

```json
// 标签名为空
{ "code": 400, "message": "标签名不能为空", "data": null }
// 超过 4 个字
{ "code": 400, "message": "标签最多 4 个字", "data": null }
// 已有 20 个
{ "code": 400, "message": "标签最多 20 个", "data": null }
// 重复添加
{ "code": 400, "message": "标签已存在", "data": null }
```

前端注意事项：  新增成功后重新调用接口 22 刷新列表（本接口不返回列表）。建议前端也做一次本地判重，提升体验。

后端注意事项：  后端会 trim 后再判重，所以传 `" 运动 "` 和 `"运动"` 视为同一个标签。

---

### 接口 24：删除标签

| 项目        | 内容        |
| --------- | --------- |
| 方法        | DELETE    |
| 路径        | /api/tags |
| 是否需要Token | 是         |

请求参数（Query）：`DELETE /api/tags?tagName=内耗`

| 字段      | 类型     | 必填  | 说明            |
| ------- | ------ | --- | ------------- |
| tagName | string | 是   | 要删除的标签名（精确匹配） |

成功响应：

```json
{ "code": 200, "message": "操作成功", "data": null }
```

失败响应：

```json
// 标签不存在（或已被删除）
{ "code": 404, "message": "标签不存在", "data": null }
```

> **重要约定**：  删除只是把标签从「我的标签库」移除，**不会去清理历史日记上已打的该标签**。历史日记的 tags 是写入时的快照，误删标签不会破坏历史数据。

前端注意事项：  建议长按标签 chip 触发删除，并弹二次确认。中文参数由前端做 URL 编码（`encodeURIComponent`）。

---

### 接口 25：本周高频标签

| 项目        | 内容                    |
| --------- | --------------------- |
| 方法        | GET                   |
| 路径        | /api/diaries/tags/top |
| 是否需要Token | 是                     |
| 说明        | 统计本周一至今，哪些标签在日记里出现得最多 |

请求参数（Query）：`GET /api/diaries/tags/top?limit=2`

| 字段    | 类型  | 必填  | 说明                  |
| ----- | --- | --- | ------------------- |
| limit | int | 否   | 返回条数，默认 5；设计图展示 2 个 |

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": ["学习", "压力"]
}
```

| 字段   | 说明                             |
| ---- | ------------------------------ |
| data | 标签名数组，按本周出现次数从高到低排序；次数相同则最近的在前 |

> 约定：
> 
> - 统计区间为**本周一 00:00 至今天**（不是"最近 7 天"），对应设计图「本周小回顾」
> - 只统计未删除的日记
> - 本周没有任何带标签的日记时，返回空数组 `[]`，前端显示空状态即可

前端注意事项：  用于首页「本周小回顾 → 高频标签」卡片。设计图展示 2 个，所以一般传 `limit=2`；也可多取几个做横向滚动。

后端注意事项：  数据来自 `diary.tags`（JSON 列），用 MySQL 8.0 的 `JSON_TABLE` 拆解统计。若后续要改成"最近 7 天"口径，需同步修改后端 SQL。

---

## 附三：接口状态总览（设计界面对照 + 暂不需要标注）

### 1. 全部接口状态表

| 接口         | 路径                                  | 状态          | 对应设计页      | 说明                                                          |
| ---------- | ----------------------------------- | ----------- | ---------- | ----------------------------------------------------------- |
| 1 注册       | POST /user/register                 | ✅ 已实现       | 注册页        | 路径较 v1.4（/register）已加 /user 前缀                              |
| 2 登录       | POST /user/login                    | ✅ 已实现       | 登录页        | 同上                                                          |
| 3 添加日记     | POST /api/diaries                   | ✅ 已实现       | 今日心情       | 接入算法情感分析                                                    |
| 4 列表       | GET /api/diaries                    | ✅ 已实现       | 我的日记       | 支持 page/pageSize                                            |
| 5 详情       | GET /api/diaries/{id}               | ✅ 已实现       | 我的日记       |                                                             |
| 6 日历视图     | GET /api/diaries/calendar           | ✅ 已实现       | 情绪日历       |                                                             |
| 7 删除       | DELETE /api/diaries/{id}            | ✅ 已实现       | 我的日记       |                                                             |
| 8 统计总览     | POST /api/stats/summary             | ✅ 已实现       | 个人中心       | ⚠️ 代码为 POST（v1.4 写 GET），且用 userId 参数不校验 Token               |
| 9 趋势       | POST /api/stats/trend               | ✅ 已实现       | 情绪数据       | ⚠️ 代码为 POST（v1.4 写 GET），需手动在 Header 传 Authorization         |
| 10 分布      | GET /api/stats/emotion-distribution | ✅ 已实现       | 我的日记/情绪数据  | 返回 name/value/moodType/color                                |
| 11 词云      | GET /api/stats/word-cloud           | ⚠️ 已实现但暂不需要 | —          | **决策：词云不重要，改为用户手动打标签，前端暂不调用**                               |
| 12 热力图     | GET /api/stats/heatmap              | ✅ 已实现       | 情绪日历       |                                                             |
| 13 AI鼓励    | POST /api/ai/encourage              | ❌ 暂未实现/暂不需要 | —          | v1.4 标“可选”，代码未实现，7 张设计图无对应页                                 |
| 14 修改密码    | POST /user/password                 | ✅ 已实现       | 个人中心       | 本轮新增                                                        |
| 15 修改资料    | PUT /user/profile                   | ✅ 已实现       | 个人中心       | 本轮新增                                                        |
| 16 上传头像    | POST /user/avatar                   | ✅ 已实现       | 个人中心       | 本轮新增                                                        |
| 17 用户信息    | GET /user/info                      | ✅ 已实现       | 个人中心       | 本轮新增                                                        |
| 18 修改日记    | PUT /api/diaries/{id}               | ✅ 已实现       | 我的日记       | 本轮新增                                                        |
| 19 按日期查    | GET /api/diaries?date=              | ✅ 已实现       | 情绪日历       | 本轮新增                                                        |
| 20 心情类型    | GET /api/mood/types                 | ✅ 已实现       | 今日心情       | 本轮新增                                                        |
| 21 情绪频率    | GET /api/stats/emotion-distribution | ✅ 已实现       | 情绪数据       | 与接口 10 同路径，**靠 `days` 参数区分**（不是 range），支持 7/30/all          |
| 22 我的标签库   | GET /api/tags                       | ✅ 已实现       | 今日心情（写日记页） | v1.7 新增；首次访问自动灌入 10 个预置标签                                   |
| 23 新增标签    | POST /api/tags                      | ✅ 已实现       | 写日记页       | v1.7 新增；最多 4 字、最多 20 个                                      |
| 24 删除标签    | DELETE /api/tags                    | ✅ 已实现       | 写日记页       | v1.7 新增；只删标签库，不影响历史日记                                       |
| 25 高频标签    | GET /api/diaries/tags/top           | ✅ 已实现       | 首页本周小回顾    | v1.7 新增；统计本周一至今                                             |
| 26 关键词搜索   | GET /api/diaries?keyword=           | ✅ 已实现       | 我的日记       | v1.10 新增；MySQL 全文索引，按创建时间排序；keyword 为空返回 400                |
| 27 去年今天    | 复用接口 19                             | ✅ 无需新接口     | 今日心情侧栏     | v1.10 新增；前端调接口 19，date 传一年前的今天即可                            |
| 28 卡片上滑    | GET /api/diaries/{id}/prev-next     | ✅ 已实现       | 我的日记       | v1.10 新增；list 索引 0 为上一篇、1 为下一篇，不存在的一侧为空对象                   |
| 29 导出Excel | POST /api/export/excel              | ✅ 已实现       | 个人中心       | v1.10 新增；浏览器弹窗下载 .xlsx，含 diaryId/content/createdAt/moodType |

### 2. 已存在但暂时不需要的接口（重点标注）

| 接口               | 现状                                           | 原因                                                  | 后续                                                                                                                           |
| ---------------- | -------------------------------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **接口 11 词云**     | 后端已实现（GET /api/stats/word-cloud），但前端**暂不调用** | 词云价值有限，且依赖算法返回 keywords（当前算法不返回），已协商一致改为"用户手动打标签"替代 | 代码保留；**替代方案已在 v1.7 落地，见模块六接口 22~25（标签功能）**；**v1.8 已将 DiaryVO 的 keywords 字段移除，统一改用 tags**，本接口当前返回 `[]`；若未来做词云需先推动算法补 keywords |
| **接口 13 AI 鼓励语** | v1.4 标“可选实现”，但**当前代码未实现该接口**                 | 7 张设计稿中无“鼓励语横幅”对应页面，需求优先级低                          | 如首页后续加鼓励语横幅再实现                                                                                                               |

### 3. 纯前端即可、无需后端接口的功能（提示）

| 功能          | 说明                                  |
| ----------- | ----------------------------------- |
| 退出登录        | 前端删除本地 Token 即可，无需后端接口              |
| 系统设置（主题/语言） | 存 localStorage 即可，除非要持久化到用户表        |
| 情绪颜色对照表     | **无需维护**：接口 20 已返回每种心情的 color，前端直接用 |

### 4. ⚠️ 路径/方法一致性遗留问题（建议另立任务统一）

v1.4 早期部分接口的路径、方法与当前代码不一致，前端对接请以**代码实际**为准：

- 用户接口：v1.4 写 `/register`、`/login` → 代码实际 `/user/register`、`/user/login`
- 日记接口：v1.4 写 `/diaries` → 代码实际 `/api/diaries`
- 接口 8 summary：v1.4 写 GET → 代码实际 POST
- 接口 9 trend：v1.4 写 GET → 代码实际 POST（且需手动传 Authorization 头）