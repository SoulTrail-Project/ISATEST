# 情绪日记 API 接口文档 v1.4

> 本文档从原始 HTML 对话记录中提取，描述前端 ⇄ 后端的对外 API 接口约定。

---

版本历史：

| 版本   | 日期         | 修改人 | 说明                                          |
| ---- | ---------- | --- | ------------------------------------------- |
| v1.0 | 2025-08-01 | 全体  | 初版，确定全部接口约定                                 |
| v1.1 | 2026-08-05 | 李海宇 | 更改后端用户接口基础路径去掉api                           |
| v1.2 | 2026-08-26 | 李海宇 | 删除 moodEmoji 字段（前后端均不再使用）                   |
| v1.3 | 2026-08-26 | 李海宇 | 响应示例补齐 title 字段（接口3/4/5），请求参数表补 title 说明    |
| v1.4 | 2026-08-26 | 李海宇 | id 示例由 ObjectId 风格字符串改为数值型自增 id，对齐 MySQL 实现 |
|      |            |     |                                             |
|      |            |     |                                             |
|      |            |     |                                             |
|      |            |     |                                             |
|      |            |     |                                             |
|      |            |     |                                             |

---

## 〇、全局约定（所有接口通用）

- 基础路径： http://localhost:8080/
- 数据格式： application/json; charset=utf-8
- 统一响应格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { }
}
```

- 业务状态码：

| code | 含义          | HTTP状态码 |
| ---- | ----------- | ------- |
| 200  | 成功          | 200     |
| 400  | 参数错误        | 400     |
| 401  | 未登录/Token过期 | 401     |
| 403  | 无权限         | 403     |
| 404  | 资源不存在       | 404     |
| 500  | 服务器内部错误     | 500     |

- 认证方式： 除接口1、2外，所有接口请求头携带 Authorization: Bearer <token>

---

## 模块一：用户认证

---

### 接口 1：用户注册

| 项目        | 内容        |
| --------- | --------- |
| 方法        | POST      |
| 路径        | /register |
| 是否需要Token | 否         |

请求参数（Body）：

```json
{
  "username": "zhangsan",
  "password": "123456",
  "confirmPassword": "123456",
  "nickname": "张三"
}
```

| 字段              | 类型     | 必填  | 限制                      |
| --------------- | ------ | --- | ----------------------- |
| username        | string | 是   | 3~16位，只能包含字母、数字、下划线     |
| password        | string | 是   | 6~20位                   |
| confirmPassword | string | 是   | 必须与password一致           |
| nickname        | string | 否   | 1~20个字符，缺省时默认等于username |

成功响应：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "zhangsan",
    "nickname": "张三",
    "avatar": "",
    "createdAt": "2025-08-01T09:00:00Z"
  }
}
```

失败响应：

```json
// 用户名已被注册
{ "code": 400, "message": "用户名已存在", "data": null }
// 两次密码不一致
{ "code": 400, "message": "两次输入的密码不一致", "data": null }
// 参数校验失败
{ "code": 400, "message": "用户名长度需在3~16位之间", "data": null }
```

后端注意事项：  密码加密存储（BCrypt）， confirmPassword  只用于后端校验，不存储。

---

### 接口 2：用户登录

| 项目        | 内容     |
| --------- | ------ |
| 方法        | POST   |
| 路径        | /login |
| 是否需要Token | 否      |

请求参数（Body）：

```json
{
  "username": "zhangsan",
  "password": "123456"
}
```

成功响应：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx",
    "expiresIn": 86400,
    "userInfo": {
      "id": 1,
      "username": "zhangsan",
      "nickname": "张三",
      "avatar": "",
      "createdAt": "2025-08-01T09:00:00Z"
    }
  }
}
```

| 字段        | 说明                           |
| --------- | ---------------------------- |
| token     | JWT，有效期24小时（86400秒）          |
| expiresIn | Token有效期（秒），前端可用于本地存储时记录过期时间 |

失败响应：

```json
// 用户名或密码错误
{ "code": 400, "message": "用户名或密码错误", "data": null }
```

前端注意事项：  登录成功后把  token  和  userInfo  存入 localStorage，后续所有请求自动带上  Authorization  头。前端根据  expiresIn  提前判断Token是否过期，提前跳转登录页。

---

## 模块二：情绪日记（CRUD）

---

### 接口 3：添加日记

| 项目        | 内容       |
| --------- | -------- |
| 方法        | POST     |
| 路径        | /diaries |
| 是否需要Token | 是        |

请求参数（Body）：

```json
{
  "title": "我是标题",
  "content": "今天和朋友去了公园，心情很好，就是有点累。",
  "moodType": "happy"
}
```

| 字段       | 类型     | 必填  | 限制                                                         |
| -------- | ------ | --- | ---------------------------------------------------------- |
| title    | string | 是   | 1~50个字符，日记标题                                               |
| content  | string | 是   | 1~500字                                                     |
| moodType | string | 是   | 枚举： happy  /  sad  /  angry  /  anxious  /  tired  /  calm |

成功响应：

```json
{
  "code": 200,
  "message": "日记保存成功",
  "data": {
    "id": 7,
    "title": "我是标题",
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
      "label": "positive",
      "score": 0.87,
      "emotion": "joy"
    },
    "keywords": ["朋友", "公园", "心情", "累"],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

失败响应：

```json
// 内容为空或超过500字
{ "code": 400, "message": "日记内容需在1~500字之间", "data": null }
// moodType不合法
{ "code": 400, "message": "情绪类型不合法", "data": null }
// 未登录
{ "code": 401, "message": "请先登录", "data": null }
```

异常降级场景（重要）：

```json
{
  "code": 500,
  "message": "日记已保存，但情感分析暂不可用，请稍后在列表页刷新查看",
  "data": {
    "id": 7,
    "title": "我是标题",
    "content": "……",
    "moodType": "happy",
    "sentiment": null,
    "keywords": [],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

> 约定：  当算法服务调用失败时，日记仍然保存成功， sentiment  和  keywords  置为空，但返回码为  500  并提示“日记已保存”。前端收到500时，检查  data.id  是否存在，若存在则提示用户“日记已保存，情绪分析稍后刷新”， 避免用户以为保存失败重复提交 。

前端注意事项：  此接口算法耗时可能超过1秒，点击保存后显示 loading 状态，并防止重复点击（防抖）。

后端注意事项：  先保存日记（开启事务），再调用算法服务。若算法失败，记录日志并做降级返回。确保用户只能操作自己的日记（从 Token 解析 userId）。

---

### 接口 4：获取日记列表（分页）

| 项目        | 内容                  |
| --------- | ------------------- |
| 方法        | GET                 |
| 路径        | /diaries            |
| 是否需要Token | 是                   |
| 说明        | 获取当前登录用户的日记，按创建时间倒序 |

请求参数（Query）：

```text
GET /api/diaries?page=1&pageSize=10&startDate=2025-07-01&endDate=2025-07-31&moodType=happy
```

| 字段        | 类型     | 必填  | 说明                |
| --------- | ------ | --- | ----------------- |
| page      | int    | 否   | 页码，默认1，从1开始       |
| pageSize  | int    | 否   | 每页条数，默认10，最大50    |
| startDate | string | 否   | 开始日期  YYYY-MM-DD  |
| endDate   | string | 否   | 结束日期  YYYY-MM-DD  |
| moodType  | string | 否   | 按用户选择的情绪筛选，不传则为全部 |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "total": 56,
    "page": 1,
    "pageSize": 10,
    "totalPages": 6,
    "list": [
      {
        "id": 7,
      "title": "公园的一天",
      "content": "今天和朋友去了公园，心情很好，就是有点累。",
      "moodType": "happy",
      "sentiment": {
          "label": "positive",
          "score": 0.87,
          "emotion": "joy"
        },
        "keywords": ["朋友", "公园", "心情", "累"],
        "createdAt": "2025-08-01T10:30:00Z"
      },
      {
        "id": 6,
      "title": "考试失利",
      "content": "考试没考好，好难过。",
      "moodType": "sad",
      "sentiment": {
          "label": "negative",
          "score": 0.21,
          "emotion": "sadness"
        },
        "keywords": ["考试", "难过"],
        "createdAt": "2025-07-31T22:00:00Z"
      }
    ]
  }
}
```

> 约定：  列表中的  content  由后端直接截取前50个字（因为列表页展示摘要），完整内容通过接口5获取。附带省略号，前端无需再截取。

失败响应：

```json
// 页码不合法
{ "code": 400, "message": "页码必须大于0", "data": null }
```

前端注意事项：  分页组件页码从  page  开始，滚动加载或点击分页时更换  page  参数。查询条件变化时重置  page=1 。

后端注意事项：  查询必须带  userId  过滤，索引建议： {userId: 1, createdAt: -1} 。

---

### 接口 5：查看单条日记详情

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | GET               |
| 路径        | /api/diaries/{id} |
| 是否需要Token | 是                 |

请求参数（路径参数）：

```text
GET /api/diaries/7
```

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 7,
    "title": "我是标题",
    "content": "今天和朋友去了公园，心情很好，就是有点累。",
    "moodType": "happy",
    "sentiment": {
      "label": "positive",
      "score": 0.87,
      "emotion": "joy"
    },
    "keywords": ["朋友", "公园", "心情", "累"],
    "createdAt": "2025-08-01T10:30:00Z"
  }
}
```

失败响应：

```json
// 日记不存在
{ "code": 404, "message": "日记不存在", "data": null }
// 试图访问别人的日记
{ "code": 403, "message": "无权查看该日记", "data": null }
```

后端注意事项：  查询时同时验证  id  存在 且  userId  匹配，否则分别返回404或403。

---

### 接口 6：日历视图（按天情感汇总）

| 项目        | 内容                    |
| --------- | --------------------- |
| 方法        | GET                   |
| 路径        | /api/diaries/calendar |
| 是否需要Token | 是                     |

请求参数（Query）：

```text
GET /api/diaries/calendar?year=2025&month=8
```

| 参数    | 类型  | 必填  | 说明       |
| ----- | --- | --- | -------- |
| year  | int | 是   | 年份，如2025 |
| month | int | 是   | 月份，1~12  |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-08-01", "avgScore": 0.87, "count": 1, "mainMood": "happy" },
    { "date": "2025-08-03", "avgScore": 0.35, "count": 3, "mainMood": "sad" },
    { "date": "2025-08-05", "avgScore": 0.52, "count": 2, "mainMood": "calm" }
  ]
}
```

| 字段       | 说明                            |
| -------- | ----------------------------- |
| date     | 日期，格式  YYYY-MM-DD             |
| avgScore | 当天所有日记情感得分的平均值（0~1）           |
| count    | 当天日记条数                        |
| mainMood | 当天出现次数最多的 moodType（用户主动选择的情绪） |

> 约定：  没有写日记的日期 不返回 （前端默认显示空白）。 avgScore >= 0.6  为积极， 0.4 ~ 0.6  为中性， < 0.4  为消极，用于前端给日历格子着色。

前端注意事项：  用返回的  date  字段直接映射到日历组件。不需要补全整个月的每一天。

后端注意事项：  若一天有多个日记，取  moodType  出现次数最多的那个；若并列，取最近一条的。

---

### 接口 7：删除日记

| 项目        | 内容                |
| --------- | ----------------- |
| 方法        | DELETE            |
| 路径        | /api/diaries/{id} |
| 是否需要Token | 是                 |

请求参数（路径参数）：

```text
DELETE /api/diaries/7
```

成功响应：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

失败响应：

```json
// 日记不存在
{ "code": 404, "message": "日记不存在", "data": null }
// 权限不足
{ "code": 403, "message": "无权删除该日记", "data": null }
```

前端注意事项：  删除操作需要二次确认弹窗（防误删）。删除成功后，本地列表移除该条并刷新统计页数据。

---

## 模块三：统计与可视化

---

### 接口 8：统计总览

| 项目        | 内容                 |
| --------- | ------------------ |
| 方法        | GET                |
| 路径        | /api/stats/summary |
| 是否需要Token | 是                  |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalDiaryCount": 56,
    "activeDays": 30,
    "sentimentRatio": {
      "positive": 0.55,
      "neutral": 0.25,
      "negative": 0.20
    },
    "averageScore": 0.62,
    "streakDays": 5,
    "longestStreak": 12,
    "currentMoodSummary": {
      "mostFrequentMood": "happy",
      "mostFrequentMoodCount": 18
    }
  }
}
```

| 字段                 | 说明                     |
| ------------------ | ---------------------- |
| totalDiaryCount    | 总日记数                   |
| activeDays         | 写日记的总天数                |
| sentimentRatio     | 积极/中性/消极占比，总和为1        |
| averageScore       | 所有日记平均情感得分             |
| streakDays         | 当前连续写日记天数（今天或昨天有记录才计算） |
| longestStreak      | 历史最长连续天数               |
| currentMoodSummary | 出现频率最高的 moodType 及次数   |

前端注意事项：  这个数据用于“我的统计”页顶部卡片展示。 sentimentRatio  可直接传入 ECharts 饼图。

后端注意事项： streakDays  计算逻辑：从今天开始往前数，如果今天是最后一天且当天有日记则算，否则从昨天开始。连续中断则停止。这个逻辑容易出错，建议单独写工具类并测试。

---

### 接口 9：情绪趋势（折线图）

| 项目        | 内容               |
| --------- | ---------------- |
| 方法        | GET              |
| 路径        | /api/stats/trend |
| 是否需要Token | 是                |

请求参数（Query）：

```text
GET /api/stats/trend?range=7
```

| 参数    | 类型  | 必填  | 说明              |
| ----- | --- | --- | --------------- |
| range | int | 是   | 7 或 30，其他值返回400 |

成功响应（range=7）：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-07-26", "avgScore": 0.45, "diaryCount": 2 },
    { "date": "2025-07-27", "avgScore": 0.62, "diaryCount": 1 },
    { "date": "2025-07-28", "avgScore": 0.30, "diaryCount": 3 },
    { "date": "2025-07-29", "avgScore": 0.81, "diaryCount": 2 },
    { "date": "2025-07-30", "avgScore": 0.55, "diaryCount": 1 },
    { "date": "2025-07-31", "avgScore": 0.48, "diaryCount": 2 },
    { "date": "2025-08-01", "avgScore": 0.77, "diaryCount": 2 }
  ]
}
```

> 约定：  不写日记的日期也返回， avgScore  为  null ， diaryCount  为  0 。这样前端折线图能显示断点，不需要前端自己补全日期。

前端注意事项：  折线图X轴直接用  date ，Y轴用  avgScore 。 null  值用 ECharts 的  connectNulls: false  显示断线。

---

### 接口 10：情绪分布（饼图）

| 项目        | 内容                              |
| --------- | ------------------------------- |
| 方法        | GET                             |
| 路径        | /api/stats/emotion-distribution |
| 是否需要Token | 是                               |

请求参数：  无

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "name": "开心", "value": 18, "moodType": "happy", "color": "#FFD700" },
    { "name": "平静", "value": 10, "moodType": "calm", "color": "#87CEEB" },
    { "name": "焦虑", "value": 12, "moodType": "anxious", "color": "#FFA500" },
    { "name": "难过", "value": 8, "moodType": "sad", "color": "#6495ED" },
    { "name": "愤怒", "value": 5, "moodType": "angry", "color": "#FF4500" },
    { "name": "疲惫", "value": 3, "moodType": "tired", "color": "#808080" }
  ]
}
```

| 字段       | 说明                      |
| -------- | ----------------------- |
| name     | 中文名称，用于前端显示图例           |
| value    | 该情绪出现的次数（按 moodType 统计） |
| moodType | 英文枚举值，用于前端做颜色映射兜底       |
| color    | 建议颜色，前端也可以用自己的映射不用此字段   |

前端注意事项：  支持 ECharts 饼图的  data.name  /  data.value  直接传入。可增加显示百分比。

后端注意事项：  按  moodType  分组统计所有日记，只统计用户自己的记录。

---

### 接口 11：词云数据

| 项目        | 内容                    |
| --------- | --------------------- |
| 方法        | GET                   |
| 路径        | /api/stats/word-cloud |
| 是否需要Token | 是                     |

请求参数：  无（返回全部历史日记的关键词汇总）

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "word": "焦虑", "value": 24 },
    { "word": "失眠", "value": 18 },
    { "word": "考试", "value": 15 },
    { "word": "开心", "value": 12 },
    { "word": "朋友", "value": 9 },
    { "word": "运动", "value": 7 }
  ]
}
```

| 字段    | 说明              |
| ----- | --------------- |
| word  | 关键词文本           |
| value | 词频（所有日记中出现次数总和） |

> 约定：  只返回出现次数 ≥ 3 的词，且最多返回 50 个，按  value  从高到低排序。过滤掉无意义的停用词（“的”、“了”、“我”等，算法组在提取时就过滤掉）。

前端注意事项：  直接传入  echarts-wordcloud  的  data  属性即可。词云大小建议使用  value  映射  textSize 。若数组为空，前端显示“写几篇日记后再来看看你的心情词云吧”空状态提示。

后端注意事项：  这个查询较重的做法是每次实时聚合所有日记的关键词，小数据量没问题。若数据量大可定期缓存（如每天一次）。为了演示速度够快即可。

---

### 接口 12：日历热力图

| 项目        | 内容                 |
| --------- | ------------------ |
| 方法        | GET                |
| 路径        | /api/stats/heatmap |
| 是否需要Token | 是                  |

请求参数（Query）：

```text
GET /api/stats/heatmap?month=2025-08
```

| 参数    | 类型     | 必填  | 说明          |
| ----- | ------ | --- | ----------- |
| month | string | 是   | 格式  YYYY-MM |

成功响应：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    { "date": "2025-08-01", "score": 0.87, "count": 1 },
    { "date": "2025-08-02", "score": null, "count": 0 },
    { "date": "2025-08-03", "score": 0.35, "count": 3 }
  ]
}
```

| 字段    | 说明                        |
| ----- | ------------------------- |
| date  | YYYY-MM-DD                |
| score | 当天平均情感得分（0~1），没有日记则为 null |
| count | 当天日记条数                    |

> 说明：  热力图是给日历图上色用的，返回该月每一天的数据。 score  为空表示该天无日记，前端可显示灰色或不显示。颜色映射：0 0.4 红，0.4 0.6 黄，0.6~1 绿。此接口与接口6相似，区别是：接口6返回的是 用户主动选择的情绪 （moodType）用于显示表情图标，本接口返回的是 情感分析得分 用于给色块上色。

前端注意事项：  热力图组件可用 GitHub 贡献图样式或普通日历组件，根据  score  值映射颜色深浅。

---

## 模块四：AI 扩展功能

---

### 接口 13：生成鼓励语（可选实现）

| 项目        | 内容                                     |
| --------- | -------------------------------------- |
| 方法        | POST                                   |
| 路径        | /api/ai/encourage                      |
| 是否需要Token | 是                                      |
| 说明        | 根据用户最近7天的日记情感趋势，生成一句个性化的鼓励语（可作为首页顶部横幅） |

请求参数（Body）：  无需传参（后端根据用户历史数据自行分析）

```json
{}
```

成功响应：

```json
{
  "code": 200,
  "message": "生成成功",
  "data": {
    "message": "最近你记录了很多关于考试的压力，试着每天给自己10分钟放空的时间，你已经做得很棒了。",
    "tone": "caring",
    "basedOn": {
      "positiveCount": 2,
      "negativeCount": 5,
      "dominantEmotion": "anxious"
    }
  }
}
```

| 字段      | 说明                                        |
| ------- | ----------------------------------------- |
| message | 生成的鼓励文本，前端直接展示                            |
| tone    | 语气类型（caring / cheerful / calm），前端可配相应文案样式 |
| basedOn | 后端生成逻辑的依据，便于前端调试                          |

实现方式（不调用大模型的简单规则版）：  后端统计最近7天：

- 如果积极（positive）占多数 → 返回 “你的情绪正在变好，继续保持呀！”
- 如果消极（negative）占多数 → 提取高频关键词，拼成类似“最近你常提到“考试”，记得给自己一些休息时间，你已经很努力了。”
- 如果中性为主 → “生活就像白开水，平淡也是一种美好。”

失败响应：

```json
// 数据不足（最近7天没有日记）
{ "code": 400, "message": "写几篇日记才能生成专属鼓励哦", "data": null }
```

---

## 附一：协议中几个容易忽略的约定

1. 日期格式： 接口输入统一 YYYY-MM-DD ，后端返回统一 YYYY-MM-DDTHH:mm:ssZ （ISO 8601 UTC时间）。
2. 字段命名： 接口 JSON 字段一律使用 camelCase （如 moodType ），数据库字段可自由设计在内部转换。
3. 空值处理： 字段可能为空的，统一返回 null ，不要返回空字符串（接口6、12的 score 为空时返回 null ）。
4. 列表排序： 所有列表接口默认按 createdAt 倒序。
5. 内容截断： 问题：列表中的 content 返回前50字时，若恰好切在emoji中间会乱码，建议后端按 字符 截取而不是按字节截取，并在末尾加“…”（共53个字符）。
6. id 类型： 所有 id（用户 id / 日记 id）为数值型（MySQL BIGINT 自增），JSON 中以 **数字** 返回，无引号。前端不要按字符串处理。

## 附二：接口依赖关系图（开发顺序）

```text
接口1、2（注册登录）
    ↓
接口3（添加日记） ← 依赖算法服务
    ↓
接口4、5、7（列表/详情/删除） → 接口6、8、9、10、11、12（统计，依赖日记数据）
    ↓
接口13（AI鼓励，依赖统计结果）
```

开发建议：

- 第一阶段（第1周）：实现接口1、2、3、4、5、7 → 完成“能写日记、能看日记、能删日记”
- 第二阶段（第2周）：实现接口6、8、9、10、11、12 → 完成图表展示
- 第三阶段（第3周）：实现接口13，优化算法效果 → 加分项
- 第四阶段（第4周）：联调、完善异常处理、写测试

---

这份文档写完后，建议团队做两件事：

1. 把这份 Markdown 内容导入 Apifox ，添加 Mock 规则，前端第二天就能开始开发。
2. 每个接口标注负责人：前端谁对接、后端谁实现、算法谁支撑、预估完成日期，方便进度跟踪。

如果这份模板对你们有帮助，我可以继续补充“算法服务对后端”的内部接口文档（虽然前端看不到，但后端和算法组需要）。需要的话告诉我。

---

---

# 算法服务接口文档 v1.0（后端 ⇄ 算法组）

适用对象：  后端开发、算法开发 关联文档：  对外API文档 v1.0（前端 ⇄ 后端） 版本历史：

| 版本   | 日期         | 修改人 | 说明          |
| ---- | ---------- | --- | ----------- |
| v1.0 | 2025-08-01 | 全体  | 初版，约定全部算法接口 |

---

## 〇、服务基本信息

| 项目     | 内容                              |
| ------ | ------------------------------- |
| 服务名称   | emotion-analysis-service        |
| 服务地址   | http://localhost:8081 （开发环境）    |
| 协议     | HTTP/1.1                        |
| 数据格式   | application/json; charset=utf-8 |
| 主端口    | 8081                            |
| 健康检查端口 | 8081/health                     |
| 部署方式   | 独立进程（如Docker容器）或与后端同机运行         |
| 开发语言   | Python（推荐）3.9+                  |
| 推荐框架   | FastAPI（自带并发，性能好）或 Flask        |

> ⚠️  核心约定：  算法服务的 输入是纯文本 ， 输出是结构化JSON 。后端不关心算法内部用的是什么模型（SnowNLP、HanLP、微调BERT、甚至直接调用大模型都行），只要求接口稳定。

---

## 一、核心接口：文本分析

这是整个算法服务的唯一核心入口，后端在“保存日记”时调用。

### 1.1 接口信息

| 项目     | 内容                       |
| ------ | ------------------------ |
| 方法     | POST                     |
| 路径     | /analyze                 |
| 超时时间   | ≤ 2秒 （超过则后端判为超时，走降级逻辑）   |
| 支持并发   | 至少10 QPS（后面的日记列表页可能批量调用） |
| 是否需要鉴权 | 否（仅内网部署，通过防火墙隔离）         |

### 1.2 请求参数（Body）

```json
{
  "text": "我今天好焦虑，晚上又失眠了，考试快到了。",
  "requestId": "6634f7a1b2c3d4e5f6a7b8c9"
}
```

| 字段        | 类型     | 必填  | 限制     | 说明                        |
| --------- | ------ | --- | ------ | ------------------------- |
| text      | string | 是   | 1~500字 | 要分析的日记正文                  |
| requestId | string | 是   | 长度≤64  | 日记ID或UUID，用于日志追踪（算法组排障时用） |

### 1.3 成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "sentiment": {
      "label": "negative",
      "score": 0.82,
      "emotion": "anxious",
      "confidence": 0.93
    },
    "keywords": [
      { "word": "焦虑", "weight": 0.85 },
      { "word": "失眠", "weight": 0.72 },
      { "word": "考试", "weight": 0.61 }
    ],
    "processingTimeMs": 45
  }
}
```

字段详细说明：

| 字段                        | 类型     | 说明                                                                                  |
| ------------------------- | ------ | ----------------------------------------------------------------------------------- |
| code                      | int    | 0表示成功 ，非0表示失败（见1.5）                                                                 |
| data.sentiment.label      | string | 情感大类： positive  /  neutral  /  negative                                             |
| data.sentiment.score      | float  | 0~1，越接近0越消极，越接近1越积极                                                                 |
| data.sentiment.emotion    | string | 细分情绪，枚举： joy  /  calm  /  sadness  /  anger  /  anxiety  /  fatigue ，无法判断时为 unknown |
| data.sentiment.confidence | float  | 模型置信度 0~1，供后端记录和调试展示                                                                |
| data.keywords             | array  | 关键词列表， 按权重从高到低排序 ，最多10个                                                             |
| data.keywords[].word      | string | 关键词文本，已做停用词过滤，不应出现“的”“了”“我”等                                                        |
| data.keywords[].weight    | float  | 0~1的相对权重，用于前端词云大小映射                                                                 |
| data.processingTimeMs     | int    | 算法内部耗时（毫秒），便于后端监控性能                                                                 |

### 1.4 注释示例（供后端对照）

一次真实调用链路：

```text
后端保存日记 → POST http://localhost:8081/analyze
Body: {"text": "今天和朋友去了公园，心情很好。", "requestId": "6634..."}
响应:
{
  "code": 0,
  "data": {
    "sentiment": { "label": "positive", "score": 0.87, "emotion": "joy", "confidence": 0.91 },
    "keywords": [ {"word": "朋友", "weight": 0.81}, {"word": "公园", "weight": 0.68} ],
    "processingTimeMs": 38
  }
}
```

### 1.5 失败响应（错误码约定）

| code | HTTP状态 | 含义                   | 后端处理策略                                          |
| ---- | ------ | -------------------- | ----------------------------------------------- |
| 0    | 200    | 成功                   | 正常解析                                            |
| 1001 | 400    | text为空或超长            | 重新校验入参；若text正常，则记录算法组Bug                        |
| 1002 | 422    | 文本语言不支持（如全是数字/符号/英文） | 后端落库时填默认值：label=neutral, score=0.5, keywords=[] |
| 5000 | 500    | 算法内部错误（模型加载失败、异常）    | 走降级逻辑：日记仍然保存，sentiment置null                     |
| -1   | 503    | 服务过载（排队超时）           | 走降级逻辑，并告警                                       |

失败响应示例：

```json
{
  "code": 1002,
  "message": "text is empty or unsupported language",
  "data": null
}
```

> ⚠️  重要：  算法服务 永远不能因为异常让后端崩溃 。所有未预料的异常必须捕获，返回 code: 5000 或 -1 ，HTTP状态码对应。后端对任何非200的响应，统一走“降级”逻辑。

### 1.6 算法组必须保证的边界条件

| 输入情况                        | 应该返回                  |
| --------------------------- | --------------------- |
| 空字符串  ""                    | code=1001             |
| 全是标点符号  "！！！？？？"            | code=1002，返回neutral   |
| 全是emoji  "😊😊😊"           | code=1002，返回neutral   |
| 英文  "I am very happy today" | 若词典含英文则分析；否则code=1002 |
| 500字长文                      | 正常分析，耗时≤2秒            |
| 包含换行符/制表符                   | 分词前去除，正常分析            |

---

## 二、附加接口：批量文本分析

### 2.1 用途

统计页生成词云、月度报告时，如果历史日记没有保存过算法结果（比如早期的数据），后端可能需要批量补算。此时如果逐条调用  /analyze  会太慢，需要批量接口。

### 2.2 接口信息

| 项目   | 内容                      |
| ---- | ----------------------- |
| 方法   | POST                    |
| 路径   | /analyze/batch          |
| 超时时间 | 无硬限制（批量任务），但 单条耗时不能超过2秒 |
| 最大批量 | 50条/次                   |

### 2.3 请求参数

```json
{
  "items": [
    { "requestId": "6634...001", "text": "今天好累。" },
    { "requestId": "6634...002", "text": "和朋友打游戏很开心。" }
  ]
}
```

### 2.4 成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "results": [
      {
        "requestId": "6634...001",
        "sentiment": { "label": "negative", "score": 0.30, "emotion": "fatigue", "confidence": 0.88 },
        "keywords": [ { "word": "累", "weight": 0.9 } ]
      },
      {
        "requestId": "6634...002",
        "sentiment": { "label": "positive", "score": 0.95, "emotion": "joy", "confidence": 0.94 },
        "keywords": [ { "word": "朋友", "weight": 0.8 }, { "word": "游戏", "weight": 0.75 } ]
      }
    ]
  }
}
```

> 约定： results  数组顺序与请求顺序一致。若某一条单独失败，对应项返回  {"requestId": "...", "error": {"code": 5000, "message": "..."}} ， 不影响其他条目 。

### 2.5 后端注意

这个接口在你们当前阶段（新用户写新日记） 基本用不到 ，但如果你们导入测试数据时要批量补算，或者以后支持导入历史日记，就需要它。建议算法组实现，但优先级低。

---

## 三、附加接口：健康检查

### 3.1 用途

后端在启动时或定期调用，确认算法服务是否存活。如果服务挂了对用户影响大，后端可以提前给前端一个降级提示。

### 3.2 接口信息

| 项目  | 内容      |
| --- | ------- |
| 方法  | GET     |
| 路径  | /health |

### 3.3 成功响应

```json
{
  "status": "ok",
  "version": "1.0.0",
  "modelLoaded": true
}
```

### 3.4 失败响应

```json
{ "status": "degraded", "modelLoaded": false }
```

### 3.5 约定

- status = ok 表示服务正常； degraded 表示模型未加载（还能返回响应但分析结果不可靠）。
- HTTP状态码：正常返回200，模型未加载返回200（因为服务活着），进程完全不可用才返回503。
- 后端启动时调用一次，如果失败则打印警告日志， 不阻塞后端启动 。

---

## 四、算法服务的性能要求（必须达标）

| 指标     | 要求                            |
| ------ | ----------------------------- |
| 单条分析延迟 | ≤ 500ms （本地调用，含网络开销）；目标≤200ms |
| 并发能力   | ≥ 10 QPS（测试时用100条文本并发压测）      |
| 内存占用   | ≤ 1GB                         |
| 数字稳定性  | 相同text必须返回相同结果（可复现，禁止依赖随机数）   |
| 服务可用性  | 后端调用失败时，降级逻辑必须生效，不可影响主流程      |

> 为什么要求这么明确？因为后端是同步调用，如果算法服务卡住2秒，用户保存一篇日记要等2秒+，非常影响体验。

---

## 五、算法组交付清单（给算法同学的检查表）

算法组提交代码时必须同时提供：

1. 可运行的Python服务 ，目录结构如下：

```text
algorithm-service/
├── main.py            # FastAPI/Flask入口
├── analyzer.py        # 核心分析逻辑（情感+关键词）
├── models/            # 模型/词典存放（SnowNLP分词数据等）
├── requirements.txt   # 依赖包清单
├── test/
└── README.md          # 运行方式：pip install -r requirements.txt && python main.py
```

2. 自测脚本 ，包含以下测试用例：

```python
# 算法开发必须自测通过的用例
test_cases = [
    ("我今天好开心啊！", "positive", "joy"),
    ("我好难过，想哭。", "negative", "sadness"),
    ("今天天气不错，无话可说。", "neutral", "calm"),
    ("气死我了！", "negative", "anger"),
    ("明天要考试了，我好紧张。", "negative", "anxiety"),
    ("有点累了，但还行。", "neutral", "fatigue"),
]
```

2. 每条结果必须：label匹配+emotion匹配（或unknown），score范围在0~1。
3. README ，注明： 依赖安装命令 启动命令 默认端口 更换模型/词典的方法（便于后续维护）

---

## 六、后端怎么调用（常见实现参考）

### 6.1 用Java（Spring Boot）调用的示例

```java
@Service
public class AnalysisClient {
    // 算法服务地址，从配置文件读取
    @Value("${algorithm.service.url:http://localhost:8081}")
    private String algorithmUrl;
    private final RestTemplate restTemplate;
    public AnalysisClient(RestTemplate restTemplate) {
        this.restTemplate = restTemplate;
    }
    /**
     * 调用算法服务。失败时抛异常，由上层处理降级。
     */
    public AlgorithmResult analyze(String text, String requestId) {
        Map<String, Object> body = new HashMap<>();
        body.put("text", text);
        body.put("requestId", requestId);
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        HttpEntity<Map<String, Object>> request = new HttpEntity<>(body, headers);
        try {
            ResponseEntity<AlgorithmResponse> response = restTemplate.postForEntity(
                algorithmUrl + "/analyze",
                request,
                AlgorithmResponse.class
            );
            return response.getBody().getData();
        } catch (Exception e) {
            log.error("调用算法服务失败", e);
            throw new AlgorithmServiceException("algorithm service unavailable");
        }
    }
}
```

### 6.2 别忘记超时设置

```yaml
# application.yml
spring:
  http:
    client:
      connect-timeout: 1000ms   # 连接超时1秒
      read-timeout: 2000ms      # 读取超时2秒
```

### 6.3 后端降级策略（后端必须实现）

在“保存日记”的业务逻辑中，调用算法服务必须放在  try-catch  中：

```java
SentimentResult sentiment = null;
List<String> keywords = new ArrayList<>();
try {
    AlgorithmResult result = analysisClient.analyze(diary.getContent(), diary.getId());
    // 转换格式
    sentiment = new SentimentResult(
        result.getSentiment().getLabel(),
        result.getSentiment().getScore(),
        result.getSentiment().getEmotion()
    );
    keywords = result.getKeywords().stream()
        .map(k -> k.getWord())
        .collect(Collectors.toList());
} catch (AlgorithmServiceException e) {
    log.warn("日记{}情感分析失败，降级处理", diary.getId());
    // 降级：sentiment保持null，keywords保持空，日记仍然保存
}
// 最终保存时，sentiment和keywords可能为空
```

---

## 七、两边联调流程

### 7.1 两个组各自的职责

| 组别  | 负责内容                                                      |
| --- | --------------------------------------------------------- |
| 算法组 | 写  /analyze 、 /analyze/batch 、 /health  三个接口；用Python；自测通过 |
| 后端组 | 写HTTP调用方法；超时设置；降级逻辑；写Mock（见下）                             |

### 7.2 建议的协作方式（Mock优先）

在算法服务还没写好的时候，后端不要阻塞在等待上。后端组应该：

1. 先写好调用逻辑，指向一个 本地Mock服务 （临时用Node.js/生成一个假的JSON返回）。
2. 算法组接口完成后，后端只改一行配置（ algorithm.service.url 指向真实服务），即可联调。

这样做的好处：  两个组并行开发，后端不等的、算法不听后端的。项目最后一周再合到一起。

### 7.3 联调检查清单

- 正常文本（写中文）：返回code=0，情感和关键词合理
- 空文本、纯符号：返回错误码，后端正确处理
- 超长文本（501字）：返回1001
- 算法服务停止：后端保存日记成功，sentiment为null，不崩溃
- 算法服务恢复：后端能正常分析新日记
- 并发测试：同时发20个请求，都成功返回

---

## 八、版本升级约定

如果算法组后续优化了模型（比如从SnowNLP换成了微调BERT）：

1. 接口不能变 —— /analyze 的入参和出参结构永远保持v1.0版本不变，哪怕内部模型换了。
2. 如果确实要改接口，必须提前通知后端组并 新增版本路径 （如 /analyze/v2 ），后端切换由后端组判断，不能在原地修改。
3. 算法服务的 version 字段在 /health 中体现，方便后端在看到版本号改变时排查问题。

> 简单说： 算法组不能“悄悄改了JSON字段名”然后让后端去适应，这是大忌。  所有出参变动必须走版本升级通道。

---

## 附：一句话总结整份文档

> 后端把日记文本扔给算法服务，算法服务返回“情感标签+得分+细分情绪+关键词”，后端拿到后入库并转发给前端。接口是万里长城，两条性命：两秒超时、永不崩。-，